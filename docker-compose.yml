services:
  cms-app:
    build:
      context: ./CMS
    container_name: smanka-cms-app
    restart: always
    env_file: ./CMS/.env
    environment:
      - PORT=3000
    ports:
      - "3001:3000" # Mapping ke port 3001 agar tidak bentrok
    volumes:
      - ./CMS/prisma:/app/prisma
