services:
  cms-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smanka-cms-app
    restart: always
    env_file:
      - .env
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DATABASE_URL=postgresql://admin_sekolah:passwordku@202.52.147.214:5433/sekolah_db?schema=public&sslmode=disable&connect_timeout=30
    ports:
      - "3001:3000"
    # Volume hanya untuk logs atau upload jika diperlukan, 
    # prisma sudah ada di dalam image hasil build optimal
    # Jika Anda butuh database lokal (SQLite), aktifkan volume di bawah:
    # volumes:
    #   - ./prisma/dev.db:/app/prisma/dev.db
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

